<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".FitVietApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitViet">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FitViet"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Belt-and-suspenders for AppCompat's per-app language back-compat path (API 26-32; API 33+
             uses the framework LocaleManager directly). MainActivity is now an AppCompatActivity (see
             MainActivity.kt), which applies the locale to itself directly via attachBaseContext() —
             this service's auto-store mechanism is redundant with that but harmless to keep declared,
             and would matter again if a future activity in this app isn't AppCompat-based. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- Gate D3 — the Weekly Recap PNG export/share feature needs a content:// URI (a raw
             file:// URI can't be granted to another app's share target since Android 7). Exposes
             only app/cache/images/ per file_paths.xml, not the whole cache dir. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
