package com.fitviet.app.domain

/** Feature #7 (Gate 43) — the Dashboard volume chart's selectable time window. See
 * [DashboardStatsCalculator.rangeSeries] for what each value actually buckets. */
enum class StatsRange { WEEK, MONTH, ALL }
